# Virtual environment
.venv/
venv/

# Python cache
__pycache__/
*.py[cod]

# Build / packaging
*.egg-info/
dist/
build/

# Audit logs (contain PHI — never commit)
audit.jsonl
*.jsonl

# Secrets / mapping files (never commit)
*.csv
.env
